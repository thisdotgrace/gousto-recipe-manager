info:
  name: recipes
  type: http
  seq: 3

http:
  method: GET
  url: http://127.0.0.1:8000/recipes?offset=0&limit=50
  params:
    - name: offset
      value: "0"
      type: query
    - name: limit
      value: "50"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
