info:
  name: autocomplete-categories
  type: http
  seq: 8

http:
  method: GET
  url: http://127.0.0.1:8000/categories/autocomplete?q=chicken&limit=10
  params:
    - name: q
      value: chicken
      type: query
    - name: limit
      value: "10"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
