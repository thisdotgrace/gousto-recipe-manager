info:
  name: recipes/autocomplete
  type: http
  seq: 2

http:
  method: GET
  url: http://127.0.0.1:8000/recipes/autocomplete?q=rice&limit=80&offset=20
  params:
    - name: q
      value: rice
      type: query
    - name: limit
      value: "80"
      type: query
    - name: offset
      value: "20"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
