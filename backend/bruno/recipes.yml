info:
  name: recipes
  type: http
  seq: 1

http:
  method: GET
  url: http://127.0.0.1:8000/recipes?limit=20&offset=100
  params:
    - name: limit
      value: "20"
      type: query
    - name: offset
      value: "100"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
