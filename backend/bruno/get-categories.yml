info:
  name: categories
  type: http
  seq: 7

http:
  method: GET
  url: http://127.0.0.1:8000/categories
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
